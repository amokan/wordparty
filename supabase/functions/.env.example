# Google GenAI API Key
GOOGLE_GENAI_API_KEY=your_google_api_key_here

# Public URL for generating accessible storage URLs (not SUPABASE_ prefix)
# Use this to override the internal Docker URL with localhost
PUBLIC_STORAGE_URL=http://127.0.0.1:54321

# These are automatically provided by Supabase (internal URLs)
# SUPABASE_URL - used for database/storage operations (internal: kong:8000)
# SUPABASE_SERVICE_ROLE_KEY - used for admin access
